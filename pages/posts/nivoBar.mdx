import NivoBar from "components/NivoBar";
import css from "@styled-system/css";

export const meta = {
  title: "bar charts or nah?",
};

# I wonder how nicely the Nivo.js examples play with Next.js/MDX

<div style={{display: "flex",  flexWrap: "wrap", justifyContent: "space-around"}}>
  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar />
  </div>

  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar
      data={[
        { name: "Jim", age: 34 },
        { name: "Jam", age: 44 },
        { name: "Jum", age: 54 },
      ]}
      keys={["age"]}
      indexBy="name"
      yLabel="age"
      canvas
    />
  </div>
  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 0, flexBasis: "100%"})}>
    <NivoBar />
  </div>
  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar
      data={[
        { name: "Jim", age: 34 },
        { name: "Jam", age: 44 },
        { name: "Jum", age: 54 },
      ]}
      keys={["age"]}
      indexBy="name"
      yLabel="age"
      canvas
    />
  </div>
  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar />
  </div>
  <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar
      data={[
        { name: "Jim", age: 34 },
        { name: "Jam", age: 44 },
        { name: "Jum", age: 54 },
      ]}
      keys={["age"]}
      indexBy="name"
      yLabel="age"
      canvas
    />
  </div>
   <div style={{ height: 500}} css={css({minWidth:"400px", flexGrow: 1, flexShrink: 1, flexBasis: "400px"})}>
    <NivoBar />
  </div>
  
</div>

that's pretty cool that I can just plop a chart in the middle of a "markdown" page.
